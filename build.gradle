// Top-level build file. Configuration shared by all modules lives here.
plugins {
    id 'com.android.application' version '8.1.4' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
